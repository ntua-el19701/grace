fun main() :nothing
    var y : int[3];
    var i : int;
    fun peos(x,z : int) : int 
    {
        if y[i] > y[i+1] then return 1; else 
        

       if  i <- peos(y[i], y[i+1]) > 3 then return 1;  
        
        return 0;
    }
 
    {
         
    }