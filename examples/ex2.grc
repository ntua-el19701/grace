fun main() : nothing 

var z:int;
var x:int;
fun solve (ref  p:int; r : int) :int

 {

    p<-readInteger();
   if(p<100) then return solve(r , p); 
   else
   return p+r; 

 }
{

    x<-5;
    z<-readInteger();

    x<-solve(z,x);
    writeInteger(x);
    writeInteger(z);

}
