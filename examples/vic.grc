fun main():nothing


var pin,array: int[10];
var t:char [15];

var za,zz:char;
var f:int;

fun aris( ref z :char; l : int): int
 var x : int;
 var d : char;
 {
  

   while 2 > 3 do { x <- aris(za, 3);  x <- aris(zz, 4); }
   x <-3 ;
   $ x <- x + z + y;
   $ z <- y + p - z;
   return aris(z,1);
    
}
{
za <- 'a';
   f <- aris(za,f);
    
  
}